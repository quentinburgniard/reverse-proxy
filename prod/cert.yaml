apiVersion: cert-manager.io/v1
kind: ClusterIssuer
metadata:
  name: cert
spec:
  acme:
    server: https://acme-v02.api.letsencrypt.org/directory
    email: quentinburgniard@gmail.com
    privateKeySecretRef:
      name: cert
    solvers:
      - http01:
          ingress:
            class: nginx
